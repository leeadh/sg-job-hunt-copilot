import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SG Job Hunt Copilot — Free Resume Analyzer for Singapore",
  description: "Free ATS resume analyzer with Singapore salary benchmarks (MOM data), MyCareersFuture job matching, and government programme eligibility. Built for Singaporean jobseekers.",
  keywords: ["Singapore", "resume", "job hunt", "ATS", "salary", "MOM", "SkillsFuture", "MyCareersFuture"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
